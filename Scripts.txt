подключение к докеру для создания таблиц

docker exec -it weight_control_db psql -U weight_user -d weight_control

Введите пароль, когда запросит: weight_pass
 проверка создания таблиц  \d
выход  \q

venv_weight\Scripts\activate.bat
set DATABASE_URL=postgresql://weight_user:weight_pass@localhost:5433/weight_control
uvicorn app.main:app --reload --port 8000
Запуск бекэнд    uvicorn app.main:app --reload --port 8000

апи лидара   http://localhost:8000/api/lidar/scan

переменная    set DATABASE_URL=postgresql://weight_user:weight_pass@localhost:5433/weight_control

старт контейнера docker start weight_control_db

# Добавляем новый грузовик в базу
self.add_profile(VehicleProfile(
    name="КАМАЗ 5490",           # название
    vehicle_type="truck",        # тип
    brand="КАМАЗ",               # марка
    model="5490",                # модель
    length_m=6.8,                # длина кузова
    width_m=2.55,                # ширина кузова
    height_m=2.3,                # высота кузова
    empty_height_mm=380,         # высота бортов
    points_range=(130, 500),     # ожидаемое количество точек
    spread_range=(600, 2500)     # ожидаемый разброс
))

self.add_profile(VehicleProfile(
    name="Коробка 40x60",
    vehicle_type="box",
    length_m=0.4,
    width_m=0.6,
    height_m=0.3,
    points_range=(8, 50),
    spread_range=(30, 300),
    empty_height_mm=0
))


curl http://localhost:8000/api/health

настройка угла 
Угол	Команда
20° (-10°…+10°)	"sWN LMPoutputRange 1 5000 FFFF0B90 0B90"
30° (-15°…+15°)	"sWN LMPoutputRange 1 5000 FFFF1148 1148"
40° (-20°…+20°)	"sWN LMPoutputRange 1 5000 FFFF16A0 16A0"
50° (-25°…+25°)	"sWN LMPoutputRange 1 5000 FFFF1BF8 1BF8"
60° (-30°…+30°)	"sWN LMPoutputRange 1 5000 FFFF2120 2120"

пароль servicelevel сервис авторизация 

#---------------------GIT
# 1. Работа в develop
git checkout develop
git add .
git commit -m "fix: something"
git push origin develop

# 2. Когда всё готово — влить в main
git checkout main
git merge develop
git push origin main

# 3. Вернуться в develop
git checkout develop

python -m uvicorn main:app --host 0.0.0.0 --port 8000 --workers 1 --env-file .env